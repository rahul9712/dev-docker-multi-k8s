apiVersion: v1

kind: PersistentVolumeClaim

metadata:
  name: database-persistent-volume-claim

spec:
  accessModes:
    - ReadWriteOnce # Other values: ReadOnlyMany, ReadWriteMany
  resources:
    requests:
      storage: 2Gi
